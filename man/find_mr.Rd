% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_mr.R
\name{find_mr}
\alias{find_mr}
\title{Find mirror repeats (triplex DNA)}
\usage{
find_mr(
  seq,
  minMRrep = 10L,
  maxMRspacer = 100L,
  minTriplexYRpercent = 10L,
  maxTriplexSpacer = 8L,
  format = c("data.frame", "GRanges")
)
}
\arguments{
\item{seq}{A single DNA sequence as a character string, or a path to a
FASTA file.}

\item{minMRrep}{Minimum length of each repeat arm. Default: \code{10}.}

\item{maxMRspacer}{Maximum spacer length between arms. Default: \code{100}.}

\item{minTriplexYRpercent}{Minimum purine/pyrimidine content (\%) for the
triplex subset flag. Default: \code{10}.}

\item{maxTriplexSpacer}{Maximum spacer for triplex subset flag. Default: \code{8}.}

\item{format}{Output format: \code{"data.frame"} (default) or \code{"GRanges"}.}
}
\value{
A \code{data.frame} (or \code{GRanges}) with columns: \code{seq_name}, \code{start},
\code{end}, \code{strand}, \code{length}, \code{spacer}, \code{num_repeats}, \code{remainder},
\code{subset}.
}
\description{
Searches a DNA sequence for mirror repeats — identical sequences facing
each other on the same strand — which can form intramolecular triplex
(H-DNA) structures.
}
\references{
Cer et al. (2013) \doi{10.1093/nar/gks955}
}
\seealso{
\code{\link[=find_nonb]{find_nonb()}}
}
