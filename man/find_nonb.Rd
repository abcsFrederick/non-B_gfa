% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_nonb.R
\name{find_nonb}
\alias{find_nonb}
\title{Find all non-B DNA forming motifs}
\usage{
find_nonb(
  seq,
  format = c("data.frame", "GRanges"),
  skip = character(0),
  ir_args = list(),
  mr_args = list(),
  dr_args = list(),
  gq_args = list(),
  zdna_args = list(),
  str_args = list(),
  apr_args = list()
)
}
\arguments{
\item{seq}{A single DNA sequence as a character string, or a path to a
FASTA file.}

\item{format}{Output format for each element of the returned list:
\code{"data.frame"} (default) or \code{"GRanges"}.}

\item{skip}{A character vector of motif types to skip. Valid values:
\code{"IR"}, \code{"MR"}, \code{"DR"}, \code{"GQ"}, \code{"Z"}, \code{"STR"}, \code{"APR"}.}

\item{ir_args, mr_args, dr_args, gq_args, zdna_args, str_args, apr_args}{Named lists of additional arguments forwarded to the corresponding
\verb{find_*()} function. See each function's documentation for available
parameters.}
}
\value{
A named list with up to seven elements (\code{IR}, \code{MR}, \code{DR}, \code{GQ},
\code{Z}, \code{STR}, \code{APR}). Skipped motif types are absent from the list.
Each element is a \code{data.frame} or \code{GRanges} as controlled by \code{format}.
}
\description{
A unified wrapper that runs any combination of the seven non-B DNA motif
finders on a single DNA sequence or FASTA file and returns a named list of
results.
}
\examples{
\dontrun{
fasta <- system.file("extdata", "gfa_test.fasta", package = "nonbgfa")
results <- find_nonb(fasta)
results$IR   # inverted repeats
results$GQ   # G-quadruplexes
}
}
\references{
Cer et al. (2013) Non-B DB v2.0. \emph{Nucleic Acids Research},
41(D1):D94–D100. \doi{10.1093/nar/gks955}
}
\seealso{
\code{\link[=find_ir]{find_ir()}}, \code{\link[=find_mr]{find_mr()}}, \code{\link[=find_dr]{find_dr()}}, \code{\link[=find_gq]{find_gq()}},
\code{\link[=find_zdna]{find_zdna()}}, \code{\link[=find_str]{find_str()}}, \code{\link[=find_apr]{find_apr()}}
}
