% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_apr.R
\name{find_apr}
\alias{find_apr}
\title{Find A-phased repeats (bent DNA)}
\usage{
find_apr(
  seq,
  minATracts = 3L,
  minATractSep = 10L,
  maxATractSep = 11L,
  minAPRlen = 3L,
  maxAPRlen = 9L,
  format = c("data.frame", "GRanges")
)
}
\arguments{
\item{seq}{A single DNA sequence as a character string, or a path to a
FASTA file.}

\item{minATracts}{Minimum number of consecutive A-tracts. Default: \code{3}.}

\item{minATractSep}{Minimum center-to-center separation between A-tracts
(bp). Default: \code{10}.}

\item{maxATractSep}{Maximum center-to-center separation between A-tracts
(bp). Default: \code{11}.}

\item{minAPRlen}{Minimum number of consecutive A's in an A-tract.
Default: \code{3}.}

\item{maxAPRlen}{Maximum number of consecutive A's in an A-tract.
Default: \code{9}.}

\item{format}{Output format: \code{"data.frame"} (default) or \code{"GRanges"}.}
}
\value{
A \code{data.frame} (or \code{GRanges}) with columns: \code{seq_name}, \code{start},
\code{end}, \code{strand}, \code{length}, \code{spacer}, \code{num_repeats}, \code{remainder},
\code{subset}.
}
\description{
Searches a DNA sequence for A-phased repeats — A/T tracts recurrently
spaced at ~10–11 bp intervals (one helical turn) — which cause
macroscopic DNA bending.
}
\references{
Cer et al. (2013) \doi{10.1093/nar/gks955}
}
\seealso{
\code{\link[=find_nonb]{find_nonb()}}
}
