% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_ir.R
\name{find_ir}
\alias{find_ir}
\title{Find inverted repeats (cruciform DNA)}
\usage{
find_ir(
  seq,
  minIRrep = 6L,
  maxIRspacer = 100L,
  shortIRcut = 9L,
  shortIRspacer = 4L,
  minCruciformRep = 6L,
  maxCruciformSpacer = 4L,
  format = c("data.frame", "GRanges")
)
}
\arguments{
\item{seq}{A single DNA sequence as a character string, or a path to a
FASTA file (detected automatically if the string ends in \code{.fa}, \code{.fasta},
\code{.fna}, or \code{.fa.gz}).}

\item{minIRrep}{Minimum length of each repeat arm. Default: \code{6}.}

\item{maxIRspacer}{Maximum spacer length between arms. Default: \code{100}.}

\item{shortIRcut}{Maximum arm length to be classified as a "short" IR.
Default: \code{9}.}

\item{shortIRspacer}{Maximum spacer for short IRs. Default: \code{4}.}

\item{minCruciformRep}{Minimum arm length for cruciform subset flag.
Default: \code{6}.}

\item{maxCruciformSpacer}{Maximum spacer for cruciform subset flag.
Default: \code{4}.}

\item{format}{Output format: \code{"data.frame"} (default) or \code{"GRanges"}.}
}
\value{
A \code{data.frame} (or \code{GRanges}) with columns: \code{seq_name}, \code{start},
\code{end}, \code{strand}, \code{length}, \code{spacer}, \code{num_repeats}, \code{remainder},
\code{subset}.
}
\description{
Searches a DNA sequence for inverted repeats — complementary palindromic
sequences on the same strand separated by a spacer — which can form
cruciform structures.
}
\references{
Cer et al. (2013) Non-B DB v2.0. \emph{Nucleic Acids Research},
41(D1):D94–D100. \doi{10.1093/nar/gks955}
}
\seealso{
\code{\link[=find_nonb]{find_nonb()}} for running all motif finders at once.
}
